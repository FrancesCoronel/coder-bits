/* 
	FVCproductions
			
	Question:
		
	Write a program  which  
	
	1. declares and intializes  a char variable 
	letter  with  ’a’ and prints  it on the screen as a character 
	and as corresponding ASCII  code.

	2. declares and initializes an integer variable a with 123, 
	and prints the value of a over 7 places,

	3. declares and initializes  a char variable  letter  with  ’a’ 
	and prints  it on the screen as a character and as corresponding ASCII  code.
*/

	#include <stdio.h>

	int main() 
	
	{
	
	char c = 'a';
	int i = 97;
	
	printf("character = %c, ASCII code = %d\n", c, i);
	
	return 0;
	
	}
